/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 */

export default function App() {
  // This app uses static HTML files (index.html, about.html, etc.) 
  // served by Vite. React is not strictly necessary for the tool logic
  // but we keep this file to satisfy the build system.
  return null;
}
